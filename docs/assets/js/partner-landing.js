(()=>{const PRICE=450,$=s=>document.querySelector(s),format=n=>n.toLocaleString('es-AR');let selected=$('.package.active'),people=1,enabled=false;function render(){const full=Number(selected.dataset.kg)*people,manual=Number($('#custom-kg').value),kg=manual>0?Math.round(manual):full,name=selected.dataset.name;$('#people').textContent=people;$('#selected-name').textContent=name;$('#kg').textContent=format(kg);$('#amount').textContent=format(kg*PRICE);$('#full-reference').textContent=`Impacto completo estimado: ${format(full)} kg · $${format(full*PRICE)}`;$('#summary').textContent=manual>0?`Elegiste compensar ${format(kg)} de los ${format(full)} kg estimados.`:`Estimación completa para ${people} ${people===1?'persona':'personas'} en ${name}.`;document.documentElement.style.setProperty('--fill',Math.min(.9,(28+kg*.8)/100));$('#success').classList.remove('show')}document.querySelectorAll('.package').forEach(b=>b.onclick=()=>{document.querySelectorAll('.package').forEach(x=>{x.classList.remove('active');x.setAttribute('aria-pressed','false')});b.classList.add('active');b.setAttribute('aria-pressed','true');selected=b;render()});$('#minus').onclick=()=>{people=Math.max(1,people-1);render()};$('#plus').onclick=()=>{people=Math.min(20,people+1);render()};$('#custom-kg').oninput=render;$('#switch').onclick=()=>{enabled=!enabled;$('#switch').classList.toggle('on',enabled);$('#switch').setAttribute('aria-checked',String(enabled));$('#toggle-text').textContent=enabled?'Incluida en esta simulación':'Activala para incluirla';$('#continue').innerHTML=enabled?'Continuar con compensación <span>→</span>':'Continuar sin compensación <span>→</span>'};$('#continue').onclick=()=>$('#success').classList.add('show');const toggle=$('.nav-toggle');toggle.onclick=()=>{const open=document.body.classList.toggle('nav-open');toggle.setAttribute('aria-expanded',String(open))};const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');observer.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(x=>observer.observe(x));const track=$('.steps'),steps=[...document.querySelectorAll('.step')];let frame=0;function move(){frame=0;const r=track.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight*.62-r.top)/Math.max(1,r.height))),i=Math.min(3,Math.floor(p*4));track.style.setProperty('--journey-x',`${Math.max(0,track.clientWidth-13)*p}px`);steps.forEach((x,n)=>x.classList.toggle('passed',n<=i))}addEventListener('scroll',()=>{if(!frame)frame=requestAnimationFrame(move)},{passive:true});addEventListener('resize',move,{passive:true});render();move()})();
